# Testability Example (Section 2.3.3)

This example demonstrates how Dependency Injection improves testability.

## How to Run

1.  Make sure you have Node.js and npm installed.
2.  From this directory, run `npm install` to install Jest.
3.  Once installed, run `npm test` to execute the test.