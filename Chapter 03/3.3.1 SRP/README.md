# 3.3 Single Responsibility Principle (SRP) Examples

* `/Before`: Demonstrates the monolithic `Player` class violating SRP.
* `/After`: Shows the refactored code with separated responsibilities.

## How to Run
Navigate to either `Before` or `After` and run:
```bash

dotnet run
