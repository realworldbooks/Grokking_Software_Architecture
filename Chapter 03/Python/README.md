# Chapter 3: Coupling, Cohesion, and SOLID (Python)

Welcome to the Python companion code for **Chapter 3**. 

## What's Inside

This directory contains standalone examples mapped directly to the book sections. Each folder contains a `Before` and `After` subfolder:

1. **`/3.1 Coupling Test`** 
2. **`/3.2 SRP`** 
3. **`/3.3 OCP`**
4. **`/3.4 LSP`** 
5. **`/3.5 ISP`** 
6. **`/3.6 DIP`** 
7. **`/3.7 OrderProcessor Refactor`** ## How to Run the Code

This project is **100% zero-setup**. There are no `pip` installs or external libraries required. 

To run an example, navigate to its specific Before or After folder in your terminal and execute the `main.py` file:

```bash
# Example: Running the refactored SRP lesson
cd "3.2 SRP/After"
python main.py

Alternatively, open the python folder in an IDE like VS Code or PyCharm, open the main.py file you want to test, and click the "Run" button.