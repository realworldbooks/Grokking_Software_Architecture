# 3.3 Single Responsibility Principle (SRP) Examples

* `/BadWay`: Demonstrates the monolithic `Player` class violating SRP.
* `/GoodWay`: Shows the refactored code with separated responsibilities.

## How to Run
Navigate to either `BadWay` or `GoodWay` and run:
```bash
dotnet run